# Copy this file to `.env` (which is gitignored) and fill in your key:
#   cp .env.template .env

# Your OpenRouter key — the smolagents model in main.py uses it.
OPENROUTER_API_KEY=sk-or-your-key-here

# Optional: override the model id (defaults to openai/gpt-5.4-nano).
PARTNUH_MODEL=openai/gpt-5.4-nano
